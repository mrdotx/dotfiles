# path:   /home/klassiker/.local/share/repos/dotfiles/.config/mpv/mpv.conf
# author: klassiker [mrdotx]
# github: https://github.com/mrdotx/dotfiles
# date:   2021-09-03T08:24:50+0200

## general
# pause                                   # no autoplay
# keep-open                               # keep the player open when a file's end is reached
# save-position-on-quit                   # handled by a script
ontop
autofit=720
geometry=-+0-+0
# geometry=99%:99%
no-border                               # no window title bar
msg-module                              # prepend module name to log messages
msg-color                               # color log messages on terminal
term-osd-bar                            # display a progress bar on the terminal
cursor-autohide-fs-only                 # don't autohide the cursor in window mode, only fullscreen
cursor-autohide=1000                    # autohide the curser after 1s
prefetch-playlist=yes
force-seekable=yes
hls-bitrate=max                         # use max quality for HLS streams
rtsp-transport=udp                      # for fritzbox streaming

[default]
    ## osd
    osd-level=1                         # enable osd and display --osd-status-msg on interaction
    osd-duration=2500                   # hide the osd after x ms
    osd-status-msg="${time-pos} / ${duration}${?percent-pos:　(${percent-pos}%)}${?frame-drop-count:${!frame-drop-count==0:　Dropped: ${frame-drop-count}}}\n${?chapter:Chapter: ${chapter}}"
    osd-font="Source Sans Pro"
    osd-font-size=32
    osd-color="#ccffffff"               # ARGB format
    osd-border-color="#dd322640"        # ARGB format
    osd-bar-align-y=0                   # progress bar y alignment (-1 top, 0 centered, 1 bottom)
    osd-border-size=2                   # size for osd text and progress bar
    osd-bar-h=2                         # height of osd bar as a fractional percentage of your screen height
    osd-bar-w=60                        # width of " " "

    ## subtitles
    sub-auto=fuzzy                      # external subs don't have to match the file name exactly to autoload
    sub-file-paths-append=ass           # search for external subs in these relative subdirectories
    sub-file-paths-append=srt
    sub-file-paths-append=sub
    sub-file-paths-append=subs
    sub-file-paths-append=subtitles

    demuxer-mkv-subtitle-preroll        # try to correctly show embedded subs when seeking
    embeddedfonts=yes                   # use embedded fonts for SSA/ASS subs
    sub-fix-timing=no                   # do not try to fix gaps (which might make it worse in some cases)
    sub-ass-force-style=Kerning=yes     # allows you to override style parameters of ASS scripts
    sub-use-margins
    sub-ass-force-margins

    ## the following options only apply to subtitles without own styling (i.e. not ASS but e.g. SRT)
    sub-font="Source Sans Pro"
    sub-font-size=36
    sub-color="#ffffffff"
    sub-border-color="#ff262626"
    sub-border-size=3.2
    sub-shadow-offset=1
    sub-shadow-color="#33000000"
    sub-spacing=0.5

    ## languages
    slang=de,deu,ger,en,eng             # automatically select these subtitles (decreasing priority)
    alang=de,deu,ger,en,eng             # automatically select these audio tracks (decreasing priority)

    ## audio
    audio-channels=stereo               # alsa default is mono
    audio-file-auto=fuzzy               # external audio doesn't has to match the file name exactly to autoload
    audio-pitch-correction=yes          # automatically insert scaletempo when playing with higher speed
    volume-max=200                      # maximum volume in %, everything above 100 results in amplification
    volume=100                          # default volume, 100 = unchanged

    ## hardwar acceleration
    hwdec=auto

    ## cache
    cache=auto
    # cache-default=51200                 # size in KB
    # cache-backbuffer=25600              # size in KB

## protocol specific configuration
[protocol.https]
    cache=yes

[protocol.http]
    cache=yes

# vim:ft=config
